# Frontend

## 启动

```powershell
cd C:\Mianshi\video-evidence-agent\frontend
npm install
npm run dev
```

前端默认访问 `http://127.0.0.1:5173`，后端需要先运行在 `http://127.0.0.1:9090`。

当前工作台支持：

- 准备一组可重复的演示证据
- 查看证据时间轴
- 根据视频标识提问
- 展示有证据回答、引用和证据不足拒答
